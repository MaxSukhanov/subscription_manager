<h1>Create new product</h1>
<%= form_with model: [:admin, @product] do |f|%>
  <p>Name: <%= f.text_field :name %></p>
  <p>Description: <%= f.text_area :description %></p>
  <%= submit_tag "Create"%>
<% end %>
<%= button_to 'Back to product list', admin_products_path %>